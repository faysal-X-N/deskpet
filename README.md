# 🐾 DeskPet — 桌面宠物

> 在手机上养一只会互动的小宠物：浮在任何 App 上方，点击招手，拖拽移动，偶尔还会自己走走跳跳。

[![License: GPL v3](https://img.shields.io/badge/License-GPLv3-blue.svg)](LICENSE)
[![Android](https://img.shields.io/badge/Android-8.0%2B-blue)](https://developer.android.com)
[![Kotlin](https://img.shields.io/badge/Kotlin-2.0-purple)](https://kotlinlang.org)
[![CI](https://github.com/faysal-X-N/deskpet/actions/workflows/test.yml/badge.svg)](https://github.com/faysal-X-N/deskpet/actions)

---

## ⚠️ 免责声明（请仔细阅读）

### 1. 软件性质声明

本软件（以下简称"本应用"）是一款由 AI 辅助生成的 Android 桌面宠物应用，按**"原样"（AS IS）**提供。使用者下载、安装或使用本应用即视为已完整阅读并同意本免责声明的全部条款。如果您不同意以下任何条款，请立即停止使用并卸载本应用。

### 2. 无担保声明

在适用法律允许的最大范围内：

- 本应用**不提供任何形式的明示或默示担保**，包括但不限于对适销性、特定用途适用性、所有权及非侵权性的默示担保。
- 作者不保证本应用的功能满足您的预期，不保证本应用的运行不会中断或出现错误，不保证本应用中的缺陷会被修正。
- 本应用的所有功能、素材和代码均按"现状"提供，**使用者自行承担使用本应用的全部风险**。

### 3. 责任限制

在适用法律允许的最大范围内，**作者在任何情况下均不对以下任何情况承担责任**：

| 类别 | 具体情形 |
|------|----------|
| **设备损害** | 因安装或使用本应用导致的设备死机、卡顿、发热、电池过度消耗、系统崩溃、数据损坏或丢失 |
| **间接损失** | 任何间接、特殊、附带、惩罚性或结果性损失，包括但不限于利润损失、商誉损失、数据丢失、业务中断 |
| **第三方行为** | 通过本应用导入的第三方素材（Codex Pet 包、GIF 文件等）引发的版权纠纷、内容争议或恶意代码 |
| **系统兼容** | 因 Android 系统版本差异、厂商 ROM 定制（MIUI、ColorOS、EMUI、OneUI 等）、root 权限或第三方修改导致的异常行为 |
| **权限回收** | 国产手机厂商的后台管理策略导致的悬浮窗权限被自动回收、宠物消失、服务被杀等行为——此为手机系统行为，非本应用缺陷 |

### 4. 知识产权声明

- 本应用的源代码以 MIT 许可证开源，详见 [LICENSE](LICENSE) 文件
- 本应用中包含的示例宠物素材（spritesheet 图片及 pet.json 配置）来源于 Codex Pet 社区，版权归原作者所有
- **使用者自行导入的宠物素材，其版权责任由使用者自行承担**。请确保您拥有导入素材的合法使用权
- Codex Pet 是开源桌面宠物标准格式，本应用仅提供对该格式的兼容支持

### 5. 隐私声明

- **本应用不请求、不使用、不传输任何网络权限**
- 本应用不在后台收集、存储、上传任何用户个人信息
- 所有宠物数据（位置、大小、素材文件）均存储在设备本地存储中
- 本应用不包含任何广告 SDK、统计 SDK 或第三方追踪代码
- 本应用不读取用户的联系人、短信、通话记录、相册、位置等敏感信息

### 6. 使用规范

- 本应用仅供个人娱乐使用
- 禁止将本应用用于任何违法活动
- 禁止对本应用进行逆向工程后用于商业目的（开源代码的使用须遵守 MIT 许可证条款）
- 禁止利用本应用的悬浮窗功能实施恶意行为（如遮挡关键 UI、诱导点击等）
- 如您将本应用进行二次分发，必须在显著位置附带本免责声明全文

### 7. 条款变更

作者保留随时修改本免责声明的权利。修改后的条款自发布之日起生效。建议使用者定期查阅本页面以了解最新条款。

### 8. 法律适用

本免责声明受中华人民共和国法律管辖。因本应用产生的争议，双方应友好协商解决；协商不成的，提交有管辖权的法院裁决。

---

## 📥 下载

- **[最新 APK](https://github.com/faysal-X-N/deskpet/releases)** — GitHub Releases 页面
- **[Actions → 最新运行 → Artifacts](https://github.com/faysal-X-N/deskpet/actions)** — 每次提交自动打包

---

## 📱 中文使用说明

### 一、安装指南

#### 1.1 获取安装包

从 [Releases 页面](../../releases) 下载最新版本的 APK 安装包（文件名为 `app-debug.apk` 或类似名称）。

#### 1.2 安装步骤

1. 在手机上找到下载的 APK 文件，点击打开
2. 如果系统提示「禁止安装未知来源应用」，请根据提示进入系统设置，**允许「桌面宠物」或「文件管理器」安装未知应用**
   - 不同手机路径略有不同，通常在：设置 → 安全 → 更多安全设置 → 允许安装未知应用
3. 权限开启后返回，继续安装
4. 安装完成，桌面出现「桌面宠物」图标

> **提示**：安装过程不需要联网，可以在飞行模式下完成安装。

#### 1.3 卸载方法

长按桌面上的「桌面宠物」图标 → 选择「卸载」即可。卸载后，所有宠物数据（包括您导入的素材）将被一并删除。

---

### 二、首次使用

#### 2.1 权限引导

首次打开应用时，会弹出「欢迎使用桌面宠物」对话框，显示基本操作说明。点击「知道了」关闭对话框。

#### 2.2 悬浮窗权限（核心步骤）

宠物需要「悬浮窗」权限才能在屏幕上显示。操作流程如下：

1. 在宠物栏中点击「**开启**」按钮
2. 如果尚未授予悬浮窗权限，应用会自动跳转到权限设置页面
3. 找到「桌面宠物」→ 打开「允许显示在其他应用的上层」开关
4. 返回应用 → **自动开启悬浮窗**，无需再点一次
4. 按返回键回到应用，**再次点击「开启」**

> **各品牌手机路径参考**：
>
> | 品牌 | 权限设置路径 |
> |------|------------|
> | 小米/红米 (MIUI) | 设置 → 应用设置 → 应用管理 → 桌面宠物 → 权限管理 → 显示悬浮窗 |
> | 华为/荣耀 (EMUI) | 设置 → 应用 → 应用管理 → 桌面宠物 → 权限 → 设置单项权限 → 悬浮窗 |
> | OPPO (ColorOS) | 手机管家 → 权限隐私 → 悬浮窗管理 → 桌面宠物 |
> | vivo (OriginOS) | 设置 → 安全与隐私 → 权限管理 → 权限 → 悬浮窗 → 桌面宠物 |
> | 三星 (OneUI) | 设置 → 应用程序 → 右上角"⋮" → 特殊访问 → 在其他应用上层显示 → 桌面宠物 |
> | 原生 Android | 设置 → 应用和通知 → 特殊应用权限 → 在其他应用上层显示 → 桌面宠物 |

#### 2.3 示例宠物

首次启动后，应用会自动加载 8 只内置示例宠物（包括胡桃、芙宁娜、飞雪等），无需额外导入即可体验。

---

### 三、日常操作

#### 3.1 开启宠物

1. 打开「桌面宠物」应用
2. 在宠物列表中点击想要显示的宠物（当前选中会有弹簧缩放的阴影效果）
3. 点击「**开启**」按钮
4. 宠物出现在手机屏幕上方悬浮窗中

#### 3.2 与宠物互动

| 操作 | 效果 | 适用类型 |
|------|------|----------|
| **单击** | 宠物播放招手动画 | Codex Pet |
| **单击** | 宠物做弹跳动作 | GIF 宠物 |
| **长按后拖拽** | 移动宠物到任意位置 | 全部类型 |
| **拖拽方向** | 向左拖→向左跑 / 向右拖→向右跑 / 向上拖→跳跃 / 向下拖→招手 | 仅 Codex Pet |
| **双指捏合/扩张** | 放大或缩小宠物 | 全部类型 |
| **不做任何操作** | 约 30 秒后，宠物自己散步、蹦跳、东张西望 | 仅 Codex Pet |

#### 3.3 关闭宠物

- **方法一**：回到「桌面宠物」应用，点击「**关闭**」按钮
- **方法二**：在多任务界面（后台卡片）将应用**划掉**，悬浮窗随之消失
- **注意**：仅按 Home 键回到桌面，宠物不会消失，会继续浮在其他应用上方

#### 3.4 切换宠物

在宠物栏中点击另一只宠物 → 点击「开启」。注意：切换宠物需要先关闭再开启。每只宠物的屏幕位置和缩放比例是**独立记忆**的，切换回来时会恢复上次的位置和大小。

#### 3.5 调整宠物大小

有两种方式：

- **方式一（精确）**：在宠物栏的缩放选项中点击百分比按钮（50% / 100% / 150% / 200% / 250% / 300%），按钮高亮表示当前生效的比例
- **方式二（自由）**：在屏幕上直接用**两根手指**捏合或扩张悬浮窗中的宠物

---

### 四、导入自定义宠物

本应用支持两种宠物格式：

#### 4.1 导入 Codex Pet 素材包（推荐，有动画）

Codex Pet 是一种开源桌面宠物标准，一个素材包包含：
- `pet.json` — 宠物配置文件（名称、动画帧定义等）
- `spritesheet.webp` 或 `spritesheet.png` — 精灵表图片（8列×9行动画帧）

**导入步骤**：

1. 从 [Codex Pet 社区](https://github.com/Codexdei/petdex) 或其他来源下载 `.zip` 格式素材包
2. 打开「桌面宠物」应用
3. 点击「**导入**」→「**🎨 导入 Codex Pet 素材包**」
4. 在文件选择器中找到并选择下载的 zip 文件
5. 导入成功后，宠物出现在宠物栏中，可以正常使用

> **Codex Pet 支持的 9 种动画状态**：
>
> | 状态 | 说明 | 触发时机 |
> |------|------|----------|
> | idle | 待机/发呆 | 静止不动时默认状态 |
> | running-right | 向右跑 | 向右拖拽时 |
> | running-left | 向左跑 | 向左拖拽时 |
> | waving | 招手 | 点击宠物时 |
> | jumping | 跳跃 | 向上拖拽时 |
> | waiting | 东张西望 | 自主行为触发 |
> | running | 散步 | 自主行为触发 |
> | review | 歪头观察 | 自主行为触发 |
> | failed | 沮丧 | 保留（暂未启用） |

#### 4.2 导入 GIF 动图（简单，无动画切换）

**导入步骤**：

1. 准备一个 `.gif` 动图文件（建议尺寸不超过 500×500 像素，文件不超过 10MB）
2. 打开「桌面宠物」应用
3. 点击「**导入**」→「**📷 导入 GIF 动图**」
4. 在文件选择器中选择 GIF 文件
5. 导入成功后，宠物出现在宠物栏中

> **GIF 宠物说明**：
> - GIF 宠物仅循环播放动图内容，不会像 Codex Pet 那样区分不同动画状态
> - 点击 GIF 宠物会做一个弹跳效果，不会切换动画
> - 拖拽 GIF 宠物只会移动位置，不会切换动画
> - 如果 GIF 动图过大，导入时会提示文件过大并拒绝导入

#### 4.3 重复导入检测

如果您尝试导入的设备上已经存在的宠物（通过 `sourceId` 或文件名判断），应用会提示「该宠物已存在」，**不会重复导入**，避免浪费存储空间。

---

### 五、宠物管理

#### 5.1 删除宠物

1. 在宠物栏中**长按**要删除的宠物卡片
2. 弹出确认对话框，点击「删除」确认
3. 该宠物的素材文件和配置将被**永久删除**，无法恢复

> **注意**：删除当前正在显示的宠物不会自动关闭悬浮窗。建议先关闭悬浮窗再删除。

#### 5.2 导出单个宠物

点击宠物卡片右下角的 **分享图标**（↗），可将该宠物的素材打包为 zip 文件导出，方便分享给朋友或转移到其他设备。

#### 5.3 备份全部宠物

1. 点击右上角「**⋮**」菜单
2. 选择「**💾 备份全部**」
3. 系统生成一个 `DeskPet_backup.zip` 文件，包含所有宠物数据和素材
4. 通过系统分享菜单发送到微信、QQ、网盘等处保存

#### 5.4 恢复备份

1. 打开「桌面宠物」→ 点击「**导入**」→「**💾 恢复备份**」
2. 选择之前导出的 `.zip` 备份文件
3. 恢复完成，所有宠物回到宠物栏中
4. 已存在的宠物（通过 ID 判断）会被**自动跳过**，不会重复

> **换机迁移**：在新手机上安装本应用 → 将旧手机导出的 `DeskPet_backup.zip` 传到新手机 → 使用「恢复备份」功能 → 所有宠物一键迁移完成。

---

### 六、常见问题

#### Q1：点击「开启」后宠物不出现？

1. 检查是否授予了悬浮窗权限（见第二章 §2.2）
2. 部分手机需要在手机管家/安全中心中允许「桌面宠物」后台运行
3. 尝试重启应用后再次点击「开启」

#### Q2：宠物过一会儿就自己消失了？

这是国产手机（小米、华为、OPPO、vivo 等）的后台管理策略导致的。解决方法：

- **小米**：设置 → 应用设置 → 桌面宠物 → 省电策略 → 选择「无限制」
- **华为**：手机管家 → 启动管理 → 桌面宠物 → 允许自启和后台活动
- **OPPO**：设置 → 电池 → 应用耗电管理 → 桌面宠物 → 允许后台运行
- **vivo**：i管家 → 应用管理 → 权限管理 → 自启动 → 桌面宠物

#### Q3：导入宠物时提示「格式错误」？

- 确认 Codex Pet 素材包是 `.zip` 格式，且包含 `pet.json` 和 spritesheet 图片
- 确认 GIF 文件是真正的 GIF 格式（不是改了后缀名的其他图片）
- 尝试重新下载素材包

#### Q4：为什么我的宠物不会换动作？

只有 Codex Pet 格式的宠物支持多动画状态切换。GIF 动图导入的宠物仅循环播放，不会响应拖拽方向或自主行为来切换动画。

#### Q5：切换宠物后显示的还是旧宠物？

当前版本切换宠物需要**先点「关闭」再点「开启」**才能生效。这是已知限制。

#### Q6：应用会消耗很多电量吗？

本应用已做多项性能优化：帧解码在后台 IO 线程执行、GPU 硬件加速渲染、最低帧间隔保护。宠物静止时（idle 状态）CPU 占用极低。如果您发现异常耗电，可以在不需要宠物时点击「关闭」来停止悬浮窗服务。

---

## ✨ 功能特性

- 🪟 **悬浮窗显示** — 宠物浮在所有 App 之上，支持所有屏幕方向
- 👆 **触摸交互** — 点击招手、长按拖拽移动、双指缩放大小
- 🎬 **9 种标准动画** — 兼容 Codex Pet 标准，idle / running / jumping / waving 等
- 🤖 **自主行为** — 约 30 秒无操作后宠物会自己散步、蹦跳、东张西望
- ⚡ **性能优化** — IO 线程解码 + GPU 硬件加速 + idle 2FPS 低功耗
- 🔔 **前台服务** — Android 14+ 通知栏常驻防杀
- 🪄 **自动权限** — 点开启 → 无权限 → 跳设置 → 开权限返回 → 自动开启，不用再点
- 📦 **Codex Pet 兼容** — 支持社区 2500+ 宠物素材包导入
- 🖼️ **GIF 支持** — 直接导入 GIF 动图即可作为宠物
- 💾 **备份恢复** — 一键导出/恢复全部宠物数据，方便换机迁移
- 🎯 **独立记忆** — 每只宠物独立记忆屏幕位置和缩放大小
- 🎓 **内置教程** — 顶栏 ❓ 按钮随时查看使用说明和下载链接
- 🔒 **隐私优先** — 无网络权限，纯本地运行，不收集任何数据
- 🔄 **CI/CD** — GitHub Actions 自动测试 + 自动打包 APK
- 🧪 **含测试** — 46 项自动化测试覆盖核心逻辑

---

## 🛠 技术栈

| 技术 | 用途 |
|------|------|
| Kotlin 2.0 | 开发语言 |
| Jetpack Compose | UI 框架 |
| Compose Canvas | 悬浮窗宠物渲染 |
| WindowManager | 悬浮窗管理 |
| DataStore Preferences | 本地持久化存储 |
| BitmapRegionDecoder | 精灵表逐帧解码（GPU 显存） |
| Foreground Service | Android 14+ 前台服务常驻 |

**最低要求**：Android 8.0（API 26）｜**目标版本**：Android 14（API 34）

---

## 🏗 构建

```bash
# 克隆仓库
git clone https://github.com/faysal-X-N/deskpet.git
cd deskpet

# 构建 Debug APK
./gradlew :app:assembleDebug

# 运行单元测试
./gradlew :app:testDebugUnitTest

# 运行 UI 测试（需要连接设备或模拟器）
./gradlew :app:connectedAndroidTest
```

用 Android Studio（Hedgehog 或更新版本）打开项目目录即可直接编译运行。

---

## 📂 项目结构

```
app/src/main/java/com/deskpet/
├── MainActivity.kt              # 唯一 Activity，Compose 入口
├── DeskPetApp.kt                # Application（初始化数据仓库）
├── ui/
│   ├── PetListScreen.kt         # 宠物栏列表（LazyVerticalGrid）
│   ├── ImportScreen.kt          # 导入界面（Codex Pet / GIF / 备份恢复）
│   └── theme/                   # Compose 主题（Color / Type / Theme）
├── overlay/
│   ├── PetOverlayService.kt     # 悬浮窗 Service（WindowManager + 生命周期管理）
│   ├── PetOverlayRenderer.kt    # Compose Canvas 渲染器
│   └── GestureHandler.kt        # 手势处理（点击/长按拖拽/双指缩放）
├── engine/
│   ├── PetEngine.kt             # 引擎入口（策略模式：Codex Pet / GIF 分发）
│   ├── SpritesheetParser.kt     # 精灵表解析（BitmapRegionDecoder + GPU 显存）
│   ├── GifRenderer.kt           # GIF 帧解码
│   ├── AnimationController.kt   # 动画状态机 + IO 线程帧解码 + 16ms 帧率保护
│   ├── IAnimationStateController.kt  # 动画控制接口（便于测试）
│   └── AutonomousBehavior.kt    # 自主行为调度（随机间隔触发散步/跳跃等）
├── data/
│   ├── PetRepository.kt         # 宠物数据仓库（CRUD + 位置/缩放保存）
│   ├── PetStore.kt              # DataStore 封装（JSON 序列化存储）
│   ├── IPetStore.kt             # 存储接口（便于测试）
│   ├── FileManager.kt           # 文件管理（导入/导出/备份/ZIP 压缩）
│   ├── SamplePetLoader.kt       # 示例宠物自动加载（assets 目录）
│   └── model/
│       ├── PetInfo.kt           # 宠物元数据（ID/类型/名称/位置/缩放/去重ID）
│       ├── CodexPetConfig.kt    # pet.json 反序列化模型
│       ├── PetAnimationState.kt # 9 种标准动画状态枚举
│       ├── PetType.kt           # 宠物类型枚举（CODEX_PET / GIF）
│       └── DragDirection.kt     # 拖拽方向枚举（LEFT/RIGHT/UP/DOWN）
```

---

## 📋 已知限制

| 限制 | 说明 | 影响范围 |
|------|------|----------|
| ROM 后台管理 | MIUI / ColorOS / EMUI 等可能在后台自动关闭悬浮窗 | 需将应用加入白名单（见常见问题 Q2） |
| 切换宠物 | 切换宠物后需手动关闭再开启 | 操作多一步，后续版本优化 |
| 单只显示 | 同一时间屏幕上只能显示 1 只宠物 | 多只同屏功能规划中 |
| 开机不启动 | 手机重启后需要手动打开应用 | 已规划开机自启功能 |
| 低端设备 | API 26 设备上 spritesheet 采用全量解码，内存占用约 11MB | 影响极小（该版本设备占比 <2%） |

---

## 📄 许可证

[GNU General Public License v3.0](LICENSE)

Copyright © 2026 DeskPet Contributors

本应用是自由软件：你可以自由地共享和修改它，但必须遵守 GNU GPL v3 的条款。分发包（包括修改版）必须同样以 GPL v3 许可证发布，并保持开源。
